# steam-.acf-generator-
steam .acf / appmanifest generator , automatic

automatic the script automaticly finds the folder, automatically finds the id and automatically make the file.  
just run the script with python
